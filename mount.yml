
- name: "Create root directory"
  file:
      path: /mnt/boot
      state: directory
      recurse: yes
      # owner: foo
      # group: foo

- name : "mount root"
  mount:
      path: /mnt
      src: /dev/sda3
      # fstype: iso9660
      # opts: ro,noauto
      state: mounted

- name: "Create boot directory"
  file:
      path: /mnt/boot
      state: directory
      recurse: yes
      # owner: foo
      # group: foo

- name : "mount boot"
  mount:
      path: /mnt/boot
      src: /dev/sda3
      # fstype: iso9660
      # opts: ro,noauto
      state: mounted


